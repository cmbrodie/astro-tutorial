---
const { url, title } = Astro.props;
---

<li class="blog-post">
  <a href={url}>{title}</a>
</li>

<style>
  .blog-post {
    margin: 0.375rem;
    list-style: inherit;
  }
  .blog-post a {
    padding: 0.25rem 0.5rem;
    border-radius: 0.25rem;
    font-weight: 600;
    text-decoration: none;
    color: inherit;
  }
  .blog-post a:hover,
  .blog-post a:active {
    color: var(--astro-purple-light);
    background-color: var(--astro-purple);
    box-shadow: 1px 1px 4.5px var(--astro-orange);
  }
</style>
