---
import '../styles/global.css'

---
  
  <div class="hero">
    <div class="inner-container">
        <div class="text-container">
          <h2 class="header">Get out of this world, without leaving home</h2>
          <p class="text">Explore the cosmos from your backyard. Discover galaxies, nebulae, stars, and more with our intuitive platform.</p>
          <div class="button-container">
            <a href="#" class="button">Get started</a>
            <a href="#" class="link">Learn more <span aria-hidden="true">→</span></a>
          </div>
      </div>
    </div>
  </div>
  
  <style>
    /* .container {
        background-color: var(--astro-purple-header);
      background-image: url('../images/wallace_gromit.jpg');
      background-repeat: no-repeat;
      background-size: cover;
      background-position: center;
      background-blend-mode: multiply; 
    } */

.hero {
  background: linear-gradient(
    105deg,
    hsla(0, 0%, 97%, 0.5) 10%,
      var(--astro-purple-med) 25%,
      hsla(0, 0%, 97%, 0.5) 40%,

      hsla(0, 0%, 97%, 0.5) 60%,
      var(--astro-purple-med) 75%,
      hsla(0, 0%, 97%, 0.5) 90%
       );
  width: 100%;
}


    .inner-container {
      margin: auto;
      /* max-width: 112rem;  */
      padding: 2rem 1.5rem;
    }
    /* @media (min-width: 658px) {
        .container {
            background-size: contain;
        }
    } */
    
    .text-container {
      margin: auto;
      max-width: 28rem;
      text-align: center;
    }
    .header {
      color: var(--astro-purple-dark);
      text-align: center;
    margin: 0.5rem;
    font-size: 2.25rem;
    font-family: "Karla", sans-serif;
    font-weight: 700;
    letter-spacing: 0.125rem;
    }
    .text {
      margin-top: 1.5rem; 
      font-size: 1.125rem; 
      line-height: 2; 
      color: var(--astro-purple-dark); 
    }
    .button-container {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 1.5rem;
      margin-top: 2.5rem;
    }
    .button {
      padding: 0.625rem;
      padding-left: 0.875rem;
      padding-right: 0.875rem;
      font-size: 0.875rem;
      font-weight: 600;
      background: linear-gradient(
        to top right, var(--astro-purple-dark) 20%, var(--astro-purple-hover) 90%
        );
      color: var(--astro-purple-light); 
      border-radius: 0.375rem;
      text-decoration: none;
    }
    .button:hover, .button:active {
        background: linear-gradient(
        to bottom right, var(--astro-purple-dark) 20%, var(--astro-purple-hover) 90%
        );
    }
    .link {
      font-size: 0.875rem;
      font-weight: 600;
      color: var(--astro-purple-dark);
      text-decoration: none;
      
    }
    .link:hover {
        color: var(--astro-purple);
        border-radius: .375rem;
        box-shadow: 1px 1px 1px var(--astro-purple-med);
    }
  </style>